package words 

func parseDownloadedComicsInfo(comicsInfo map[int]ComicInfo) (map[int]ComicInfo, error) {
	// check that the info is not empty and it is valid
	// stemm the info
	return comicsInfo, nil
}
